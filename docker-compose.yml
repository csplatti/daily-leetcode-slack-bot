version: 1,
services:
  bot:
    build: ./bot
    ports: 
    - 8000:8000
  backend:
    build: ./backend
    env_file: .env
    environment:
      DB_HOST: db
      DB_PORT: 8001
    depends_on:
      - db
    restart: unless-stopped

volumes:
  postgres_data:
